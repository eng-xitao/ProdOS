import { useEffect, useMemo, useState } from "react";
import { supabase } from "../lib/supabaseClient";
import { useAuth } from "../lib/AuthContext";
const CLASS_OPTIONS=[{value:"acabado",label:"Produto acabado"},{value:"materia_prima",label:"Matéria-prima"},{value:"insumo",label:"Insumo"},{value:"componente",label:"Componente / semiacabado"}];
const UNITS_FALLBACK=["un","kg","g","m","cm","l","ml"];
const money=v=>Number(v??0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
export default function ProdutosPage(){
 const {company}=useAuth();const [rows,setRows]=useState([]),[units,setUnits]=useState([]),[locations,setLocations]=useState([]);const [open,setOpen]=useState(false),[deleteOpen,setDeleteOpen]=useState(false),[editing,setEditing]=useState(false);const [deleteProduct,setDeleteProduct]=useState(null),[saving,setSaving]=useState(false),[deleting,setDeleting]=useState(false);const [error,setError]=useState(""),[deleteError,setDeleteError]=useState(""),[keyPassword,setKeyPassword]=useState("");const [form,setForm]=useState({type:"acabado",unit:"un",cfop_padrao:"5102",active:true,target_margin_percent:30,minimum_margin_percent:20,max_discount_percent:10});const isFinished=form.type==="acabado";const materialLabel=form.type==="componente"?"Componente / semiacabado":form.type==="materia_prima"?"Matéria-prima":"Insumo";
 async function load(){if(!company?.id)return;const [{data,error:e1},{data:u},{data:l,error:e3},{data:res}]=await Promise.all([supabase.from("products").select("*").eq("company_id",company.id).order("created_at",{ascending:false}),supabase.from("units_of_measure").select("code,name").order("code"),supabase.from("warehouse_locations").select("id,code,aisle,shelf,level,warehouses!inner(name,company_id)").eq("warehouses.company_id",company.id).order("code"),supabase.from("product_reserved_quantities").select("product_id,reserved_quantity").eq("company_id",company.id)]);if(e1||e3)setError((e1||e3).message);const reservedMap=Object.fromEntries((res||[]).map(r=>[r.product_id,Number(r.reserved_quantity)]));setRows((data||[]).map(r=>({...r,reserved:reservedMap[r.id]||0})));setUnits(u||[]);setLocations(l||[])}
 useEffect(()=>{load()},[company?.id]);const unitOptions=useMemo(()=>units.length?units.map(u=>({value:u.code,label:`${u.code} — ${u.name}`})):UNITS_FALLBACK.map(u=>({value:u,label:u.toUpperCase()})),[units]);const locationOptions=useMemo(()=>locations.map(l=>({value:l.id,label:[l.code,l.aisle&&`Rua ${l.aisle}`,l.shelf&&`Prat. ${l.shelf}`,l.level&&`Nível ${l.level}`].filter(Boolean).join(" · ")})),[locations]);const update=(k,v)=>setForm(f=>({...f,[k]:v}));
 function startNew(){setError("");setEditing(false);setForm({type:"acabado",unit:"un",cfop_padrao:"5102",active:true,target_margin_percent:30,minimum_margin_percent:20,max_discount_percent:10});setOpen(true)}
 function startEdit(p){setError("");setEditing(true);setForm({id:p.id,sku:p.sku||"",name:p.name||"",type:p.type||"acabado",unit:p.unit||"un",stock_quantity:p.stock_quantity||0,min_stock:p.min_stock||0,reorder_point:p.reorder_point??"",max_stock:p.max_stock??"",ncm:p.ncm||"",cfop_padrao:p.cfop_padrao||"5102",sale_price:p.sale_price||0,sale_margin_percent:p.sale_margin_percent??"",lead_time_days:p.lead_time_days||0,default_location_id:p.default_location_id||"",active:p.active!==false,target_margin_percent:p.target_margin_percent??p.sale_margin_percent??30,minimum_margin_percent:p.minimum_margin_percent??20,max_discount_percent:p.max_discount_percent??10});setOpen(true)}
 async function submit(e){e.preventDefault();if(!company?.id)return setError("Não foi possível identificar a empresa.");if(!form.name?.trim())return setError("Informe o Nome do produto.");setSaving(true);setError("");const payload={name:form.name.trim(),type:form.type,unit:form.unit||"un",min_stock:Number(form.min_stock||0),reorder_point:form.reorder_point===""?null:Number(form.reorder_point),max_stock:form.max_stock===""?null:Number(form.max_stock),ncm:form.ncm?.trim()||null,cfop_padrao:form.cfop_padrao?.trim()||null,sale_price:isFinished?Number(form.sale_price||0):0,sale_price_source:isFinished?"manual":"nao_aplicavel",sale_margin_percent:isFinished&&form.sale_margin_percent!==""?Number(form.sale_margin_percent):null,lead_time_days:Number(form.lead_time_days||0),default_location_id:form.default_location_id||null,stock_destination:isFinished?"produto_acabado":"almoxarifado",active:form.active!==false,target_margin_percent:isFinished?Number(form.target_margin_percent||0):null,minimum_margin_percent:isFinished?Number(form.minimum_margin_percent||0):null,max_discount_percent:isFinished?Number(form.max_discount_percent||0):null};const result=editing?await supabase.from("products").update(payload).eq("id",form.id).eq("company_id",company.id):await supabase.from("products").insert({...payload,company_id:company.id,cost_source:"automatico",active:true});if(result.error)setError(result.error.message);else{setOpen(false);await load()}setSaving(false)}
 async function refreshCost(id){const {data:newCost,error:e}=await supabase.rpc("refresh_product_cost",{p_product_id:id});if(e)return setError(e.message);const p=rows.find(r=>r.id===id),autoPricing=company?.sale_price_mode==="sugestao_automatica";if(p?.type==="acabado"&&autoPricing){const margin=Number(p.sale_margin_percent??p.target_margin_percent??company?.default_sale_margin_percent??0);if(margin>0&&margin<100&&Number(newCost)>0){const suggested=Number(newCost)/(1-margin/100);await supabase.from("products").update({sale_price:Number(suggested.toFixed(2)),sale_price_source:"sugestao_automatica"}).eq("id",id).eq("company_id",company.id)}}await load()}
 async function toggleActive(p){setError("");const {error:e}=await supabase.from("products").update({active:!p.active}).eq("id",p.id).eq("company_id",company.id);if(e)setError(e.message);else await load()}
 function askDelete(p){setDeleteProduct(p);setKeyPassword("");setDeleteError("");setDeleteOpen(true)}
 async function confirmDelete(e){e.preventDefault();if(!deleteProduct||!keyPassword)return setDeleteError("Informe a senha do usuário-chave.");setDeleting(true);setDeleteError("");const {data:sd,error:se}=await supabase.auth.getSession();if(se||!sd?.session){setDeleteError("Sessão inválida. Entre novamente no sistema.");setDeleting(false);return}const original=sd.session;const {data:profiles,error:pe}=await supabase.from("profiles").select("email,access_role,platform_role,is_platform_admin").eq("company_id",company.id).or("access_role.eq.key_user,access_role.eq.master,platform_role.not.is.null,is_platform_admin.eq.true");if(pe){setDeleteError(pe.message);setDeleting(false);return}const candidates=(profiles||[]).filter(p=>p.email);if(!candidates.length){setDeleteError("Nenhum usuário-chave foi configurado para esta empresa.");setDeleting(false);return}let ok=false;for(const p of candidates){const {error:ae}=await supabase.auth.signInWithPassword({email:p.email,password:keyPassword});if(!ae){ok=true;break}}if(!ok){await supabase.auth.setSession(original);setDeleteError("Senha do usuário-chave inválida.");setDeleting(false);return}const {error:de}=await supabase.from("products").delete().eq("id",deleteProduct.id).eq("company_id",company.id);await supabase.auth.setSession(original);if(de)setDeleteError(de.message.includes("foreign key")?"Este produto já possui vínculos com documentos, estoque ou produção. Torne-o inativo em vez de excluí-lo.":de.message);else{setDeleteOpen(false);setDeleteProduct(null);await load()}setDeleting(false)}
 return <div><header style={styles.header}><div><h1 style={styles.title}>Produtos</h1><p style={styles.subtitle}>Cadastro único para produtos acabados e materiais, com política comercial para orientar a negociação.</p></div><button style={styles.primary} onClick={startNew}>+ Novo</button></header>{error&&<div style={styles.error}>{error}</div>}{open&&<form onSubmit={submit} style={styles.card} className="no-print"><div style={styles.formHeader}><div style={styles.formTitle}>{editing?"Editar produto":"Novo produto"}</div>{editing&&<span style={{...styles.statusBadge,...(form.active?styles.activeBadge:styles.inactiveBadge)}}>{form.active?"Ativo":"Inativo"}</span>}</div><div style={styles.typeSwitch}><button type="button" style={{...styles.typeBtn,...(isFinished?styles.typeActive:{})}} onClick={()=>update("type","acabado")}>◆ Produto Acabado</button><button type="button" style={{...styles.typeBtn,...(!isFinished?styles.typeActive:{})}} onClick={()=>update("type","materia_prima")}>▰ Materiais</button></div><div style={styles.grid}><Field label="SKU"><input style={{...styles.input,background:"var(--panel-2)",cursor:"not-allowed"}} value={form.sku??"Gerado automaticamente ao salvar"} readOnly/></Field><Field label="Nome" required><input style={styles.input} value={form.name||""} onChange={e=>update("name",e.target.value)} required/></Field><Field label="Classe" required><select style={styles.input} value={form.type} onChange={e=>update("type",e.target.value)}>{CLASS_OPTIONS.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></Field><Field label="Unidade" required><select style={styles.input} value={form.unit||"un"} onChange={e=>update("unit",e.target.value)}>{unitOptions.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></Field><Field label="Estoque atual"><input style={{...styles.input,background:"var(--panel-2)"}} value={Number(form.stock_quantity||0).toLocaleString("pt-BR")} readOnly/></Field><Field label="Estoque mínimo"><input style={styles.input} type="number" min="0" step="any" value={form.min_stock??0} onChange={e=>update("min_stock",e.target.value)}/></Field><Field label="Ponto de pedido"><input style={styles.input} type="number" min="0" step="any" value={form.reorder_point??""} onChange={e=>update("reorder_point",e.target.value)}/></Field><Field label="Estoque máximo"><input style={styles.input} type="number" min="0" step="any" value={form.max_stock??""} onChange={e=>update("max_stock",e.target.value)}/></Field><Field label="NCM"><input style={styles.input} value={form.ncm||""} onChange={e=>update("ncm",e.target.value)}/></Field><Field label="CFOP padrão"><input style={styles.input} value={form.cfop_padrao||"5102"} onChange={e=>update("cfop_padrao",e.target.value)}/></Field><Field label="Lead time (dias)"><input style={styles.input} type="number" min="0" value={form.lead_time_days??0} onChange={e=>update("lead_time_days",e.target.value)}/></Field><Field label="Endereço padrão no Almoxarifado"><select style={styles.input} value={form.default_location_id||""} onChange={e=>update("default_location_id",e.target.value)}><option value="">Sem endereço definido</option>{locationOptions.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></Field><div style={styles.readonlyBox}><span>Custo</span><strong>{editing?money(rows.find(r=>r.id===form.id)?.cost??0):"Automático"}</strong><small>Calculado pelo sistema.</small></div>{isFinished&&<><Field label="Preço de venda"><input style={styles.input} type="number" min="0" step="0.01" value={form.sale_price??0} onChange={e=>update("sale_price",e.target.value)}/></Field><Field label="Margem alvo (%)"><input style={styles.input} type="number" min="0" max="99.99" step="0.01" value={form.target_margin_percent??30} onChange={e=>update("target_margin_percent",e.target.value)}/></Field><Field label="Margem mínima (%)"><input style={styles.input} type="number" min="0" max="99.99" step="0.01" value={form.minimum_margin_percent??20} onChange={e=>update("minimum_margin_percent",e.target.value)}/></Field><Field label="Desconto máximo (%)"><input style={styles.input} type="number" min="0" max="100" step="0.01" value={form.max_discount_percent??10} onChange={e=>update("max_discount_percent",e.target.value)}/></Field></>}</div><div style={styles.destination}><strong>Destino automático:</strong> {isFinished?"Estoque de Produto Acabado":`Almoxarifado (${materialLabel})`}</div>{isFinished&&<div style={styles.policyBox}><strong>Política comercial</strong><span>O vendedor receberá no CRM o preço alvo, o limite mínimo e o desconto máximo definidos aqui. O custo não é exibido ao vendedor.</span></div>}{editing&&<div style={styles.activeBox}><div><strong>Status do produto</strong><small>{form.active?"Produto disponível para uso nos módulos.":"Produto bloqueado para novos usos, preservando o histórico."}</small></div><button type="button" style={form.active?styles.statusOn:styles.statusOff} onClick={()=>update("active",!form.active)}>{form.active?"ATIVO":"INATIVO"}</button></div>}<div style={styles.actions}><button type="button" style={styles.secondary} onClick={()=>setOpen(false)}>Cancelar</button><button type="submit" style={styles.primary} disabled={saving}>{saving?"Salvando...":"Salvar"}</button></div></form>}
 <section style={styles.card}><div style={styles.listTitle}>Itens cadastrados <span>{rows.length}</span></div>{rows.length===0?<div style={styles.empty}>Nenhum produto cadastrado ainda.</div>:<div style={styles.tableWrap}><table style={styles.table}><thead><tr><th>SKU</th><th>Produto</th><th>Classe</th><th>Estoque</th><th>Reservado</th><th>Disponível</th><th>Custo</th><th>Preço venda</th><th>Política</th><th>Status</th><th>Ações</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} style={r.active===false?{opacity:.58}:undefined}><td><strong>{r.sku}</strong></td><td>{r.name}</td><td>{CLASS_OPTIONS.find(o=>o.value===r.type)?.label??r.type}</td><td>{Number(r.stock_quantity??0).toLocaleString("pt-BR")} {r.unit}</td><td>{r.reserved>0?<span style={styles.reservedBadge}>{r.reserved.toLocaleString("pt-BR")} {r.unit}</span>:<span style={styles.dimCell}>—</span>}</td><td>{r.reserved>0?<strong style={Number(r.stock_quantity??0)-r.reserved<0?styles.negativeAvail:undefined}>{(Number(r.stock_quantity??0)-r.reserved).toLocaleString("pt-BR")} {r.unit}</strong>:<span style={styles.dimCell}>{Number(r.stock_quantity??0).toLocaleString("pt-BR")} {r.unit}</span>}</td><td>{r.cost_source==="automatico"?<><strong>{money(r.cost)}</strong><small style={styles.auto}> automático</small></>:money(r.cost)}</td><td>{r.type==="acabado"?money(r.sale_price):"—"}</td><td>{r.type==="acabado"?<small>Alvo {Number(r.target_margin_percent??r.sale_margin_percent??0).toFixed(1)}% · Mín. {Number(r.minimum_margin_percent??0).toFixed(1)}% · Desc. {Number(r.max_discount_percent??0).toFixed(1)}%</small>:"—"}</td><td><span style={{...styles.statusBadge,...(r.active===false?styles.inactiveBadge:styles.activeBadge)}}>{r.active===false?"Inativo":"Ativo"}</span></td><td><div style={styles.rowActions}><button style={styles.actionBtn} onClick={()=>startEdit(r)}>Editar</button><button style={styles.actionBtn} onClick={()=>toggleActive(r)}>{r.active===false?"Ativar":"Inativar"}</button><button style={styles.deleteBtn} onClick={()=>askDelete(r)}>Excluir</button><button style={styles.refresh} onClick={()=>refreshCost(r.id)}>Atualizar custo</button></div></td></tr>)}</tbody></table></div>}</section>
 {deleteOpen&&<div style={styles.overlay}><form onSubmit={confirmDelete} style={styles.deleteModal}><div style={styles.deleteTitle}>Excluir produto</div><p>Você está excluindo <strong>{deleteProduct?.sku} — {deleteProduct?.name}</strong>.</p><p style={styles.warning}>Essa operação é definitiva. Para confirmar, informe a senha do usuário-chave da empresa.</p><label style={styles.field}><span style={styles.label}>Senha do usuário-chave</span><input autoFocus style={styles.input} type="password" value={keyPassword} onChange={e=>setKeyPassword(e.target.value)} autoComplete="current-password"/></label>{deleteError&&<div style={styles.error}>{deleteError}</div>}<div style={styles.actions}><button type="button" style={styles.secondary} onClick={()=>setDeleteOpen(false)}>Cancelar</button><button type="submit" style={styles.deleteConfirm} disabled={deleting}>{deleting?"Validando...":"Confirmar exclusão"}</button></div></form></div>}</div>;
}
function Field({label,help,required,children}){return <label style={styles.field}><span style={styles.label}>{label}{required?" *":""}</span>{children}{help&&<small style={styles.help}>{help}</small>}</label>}
const styles={header:{display:"flex",justifyContent:"space-between",gap:16,alignItems:"flex-start",marginBottom:18,flexWrap:"wrap"},title:{fontFamily:"var(--font-display)",fontSize:22,margin:0},subtitle:{color:"var(--text-dim)",fontSize:13,margin:"6px 0 0",maxWidth:820,lineHeight:1.55},primary:{background:"var(--blue,#2563EB)",color:"white",border:0,borderRadius:"var(--radius)",padding:"10px 16px",fontWeight:700,cursor:"pointer"},secondary:{background:"var(--panel-2)",color:"var(--text)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:"10px 16px",fontWeight:600,cursor:"pointer"},card:{background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:16,marginBottom:16},formHeader:{display:"flex",justifyContent:"space-between",alignItems:"center",gap:12,marginBottom:12},formTitle:{fontSize:16,fontWeight:800},typeSwitch:{display:"flex",gap:8,marginBottom:14,flexWrap:"wrap"},typeBtn:{background:"var(--panel-2)",border:"1px solid var(--line)",borderRadius:10,padding:"10px 14px",fontWeight:700,cursor:"pointer",color:"var(--text)"},typeActive:{border:"2px solid var(--blue,#2563EB)",padding:"9px 13px"},grid:{display:"grid",gridTemplateColumns:"repeat(3,minmax(0,1fr))",gap:12},field:{display:"flex",flexDirection:"column",gap:5},label:{fontSize:12,fontWeight:700},input:{width:"100%",boxSizing:"border-box",background:"var(--field,#F7F9FB)",color:"var(--text)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:"10px 11px",fontSize:13},help:{fontSize:10.5,color:"var(--text-dim)",lineHeight:1.35},readonlyBox:{border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:10,background:"var(--panel-2)",display:"flex",flexDirection:"column",gap:3},destination:{marginTop:14,padding:"10px 12px",border:"1px solid var(--line)",borderRadius:8,color:"var(--text-dim)",fontSize:12},policyBox:{marginTop:10,padding:"11px 12px",border:"1px solid var(--line)",borderRadius:8,display:"grid",gap:4,fontSize:12},activeBox:{marginTop:12,padding:"11px 12px",border:"1px solid var(--line)",borderRadius:8,display:"flex",justifyContent:"space-between",alignItems:"center",gap:12},statusOn:{border:0,borderRadius:999,padding:"7px 12px",fontWeight:800,cursor:"pointer",background:"#e7f6ef",color:"#16835A"},statusOff:{border:0,borderRadius:999,padding:"7px 12px",fontWeight:800,cursor:"pointer",background:"#eef0f3",color:"#667384"},statusBadge:{display:"inline-flex",alignItems:"center",borderRadius:999,padding:"4px 8px",fontSize:10,fontWeight:800},activeBadge:{background:"#e7f6ef",color:"#16835A"},inactiveBadge:{background:"#eef0f3",color:"#667384"},actions:{display:"flex",justifyContent:"flex-end",gap:8,marginTop:14},error:{marginBottom:14,padding:11,border:"1px solid #e4b5b5",background:"#fff5f5",borderRadius:8,color:"#9b2c2c",fontSize:12},listTitle:{fontSize:14,fontWeight:800,marginBottom:12},empty:{padding:24,textAlign:"center",color:"var(--text-dim)"},tableWrap:{overflowX:"auto"},table:{width:"100%",borderCollapse:"collapse",fontSize:12},auto:{color:"var(--green,#16835A)",fontSize:9},reservedBadge:{color:"var(--amber,#B7791F)",fontWeight:700},dimCell:{color:"var(--text-dim)"},negativeAvail:{color:"#b42318",fontWeight:700},rowActions:{display:"flex",gap:5,flexWrap:"wrap"},actionBtn:{background:"transparent",border:"1px solid var(--line)",borderRadius:7,padding:"6px 8px",fontSize:11,cursor:"pointer"},refresh:{background:"transparent",border:"1px solid var(--line)",borderRadius:7,padding:"6px 8px",fontSize:11,cursor:"pointer"},deleteBtn:{background:"transparent",border:"1px solid #e2b3b3",color:"#a33",borderRadius:7,padding:"6px 8px",fontSize:11,cursor:"pointer"},overlay:{position:"fixed",inset:0,background:"rgba(15,23,42,.45)",display:"flex",alignItems:"center",justifyContent:"center",padding:20,zIndex:1000},deleteModal:{width:"min(460px,100%)",background:"var(--panel)",border:"1px solid var(--line)",borderRadius:14,padding:20,boxShadow:"0 18px 60px rgba(0,0,0,.2)"},deleteTitle:{fontSize:18,fontWeight:800,marginBottom:8},warning:{fontSize:12,color:"#8a5a00",background:"#fff8e6",border:"1px solid #ead39a",borderRadius:8,padding:10,lineHeight:1.45},deleteConfirm:{background:"#b42318",color:"white",border:0,borderRadius:"var(--radius)",padding:"10px 16px",fontWeight:700,cursor:"pointer"}};