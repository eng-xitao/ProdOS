import { useEffect, useState } from "react";
import { supabase } from "../lib/supabaseClient";
import { useAuth } from "../lib/AuthContext";
import { Link } from "react-router-dom";
import logoFull from "../assets/logo-full.png";

const MATERIAL_TYPES=["materia_prima","insumo","componente"];
const MATERIAL_LABEL={materia_prima:"Matéria-prima",insumo:"Insumo",componente:"Componente"};
const EXPIRY_WARNING_DAYS=30;

export default function AlmoxarifadoPage(){
 const {company}=useAuth(); const [warehouses,setWarehouses]=useState([]),[products,setProducts]=useState([]),[locations,setLocations]=useState([]),[warehouseId,setWarehouseId]=useState(""),[typeFilter,setTypeFilter]=useState(""),[productFilter,setProductFilter]=useState(""),[levels,setLevels]=useState([]),[batches,setBatches]=useState([]),[expiringBatches,setExpiringBatches]=useState([]),[loading,setLoading]=useState(false),[error,setError]=useState("");
 const [adjustProductId,setAdjustProductId]=useState(""),[adjustType,setAdjustType]=useState("entrada"),[adjustQty,setAdjustQty]=useState(""),[adjustLocationId,setAdjustLocationId]=useState(""),[adjustBatchNumber,setAdjustBatchNumber]=useState(""),[adjustExpiryDate,setAdjustExpiryDate]=useState(""),[adjustBatchId,setAdjustBatchId]=useState("");
 async function loadWarehouses(){const {data,error:e}=await supabase.from("warehouses").select("id,name,active,warehouse_type").eq("company_id",company.id).eq("active",true).order("name");if(e)setError(e.message);else setWarehouses((data??[]).filter(w=>!/produto\s+acabado/i.test(w.name||"")));}
 async function loadProducts(){const {data,error:e}=await supabase.from("products").select("id,sku,name,unit,type").eq("company_id",company.id).in("type",MATERIAL_TYPES).eq("active",true).order("name");if(e)setError(e.message);else setProducts(data??[]);}
 async function loadExpiringBatches(){const limit=new Date();limit.setDate(limit.getDate()+EXPIRY_WARNING_DAYS);const {data}=await supabase.from("stock_batches").select("id,batch_number,expiry_date,quantity,products:product_id(sku,name,unit),warehouses:warehouse_id(name)").eq("company_id",company.id).not("expiry_date","is",null).lte("expiry_date",limit.toISOString().slice(0,10)).gt("quantity",0).order("expiry_date",{ascending:true});setExpiringBatches(data??[]);}
 async function loadLocations(wid){if(!wid){setLocations([]);return;}const {data,error:e}=await supabase.from("warehouse_locations").select("id,code").eq("company_id",company.id).eq("warehouse_id",wid).order("code");if(e)setError(e.message);else setLocations(data??[]);}
 async function loadLevels(wid){if(!wid){setLevels([]);setBatches([]);return;}setLoading(true);setError("");const [{data:stock,error:stockError},{data:batchData,error:batchError}]=await Promise.all([supabase.from("stock_levels").select("id,quantity,product_id,location_id,products:product_id(id,sku,name,unit,type),warehouse_locations:location_id(code)").eq("company_id",company.id).eq("warehouse_id",wid),supabase.from("stock_batches").select("id,product_id,batch_number,expiry_date,quantity").eq("company_id",company.id).eq("warehouse_id",wid).gt("quantity",0).order("expiry_date",{ascending:true,nullsFirst:false})]);if(stockError)setError(stockError.message);if(batchError)setError(batchError.message);const map=new Map();(products??[]).forEach(p=>map.set(p.id,{id:p.id,product_id:p.id,products:p,quantity:0,locations:[]}));(stock??[]).forEach(row=>{const p=row.products;if(!p||!MATERIAL_TYPES.includes(p.type))return;const key=p.id;if(!map.has(key))map.set(key,{id:key,product_id:key,products:p,quantity:0,locations:[]});const item=map.get(key),quantity=Number(row.quantity||0);item.quantity+=quantity;item.locations.push({id:row.location_id,code:row.warehouse_locations?.code||"Sem localização",quantity});});let rows=Array.from(map.values());if(typeFilter)rows=rows.filter(r=>r.products.type===typeFilter);if(productFilter)rows=rows.filter(r=>r.product_id===productFilter);rows.sort((a,b)=>String(a.products.name||"").localeCompare(String(b.products.name||""),"pt-BR"));setLevels(rows);setBatches(batchData??[]);setLoading(false);}
 useEffect(()=>{if(company?.id){loadWarehouses();loadProducts();loadExpiringBatches();}},[company?.id]);
 useEffect(()=>{loadLocations(warehouseId);setAdjustLocationId("");setAdjustBatchId("");loadLevels(warehouseId);},[warehouseId,typeFilter,productFilter,products.length]);
 const materialWarehouses=warehouses.filter(w=>{const name=String(w.name||"").toLowerCase(),type=String(w.warehouse_type||"").toLowerCase();return !name.includes("produto acabado")&&!type.includes("produto_acabado")&&!type.includes("produto acabado");});
 const filteredProducts=typeFilter?products.filter(p=>p.type===typeFilter):products;const selectedWarehouse=materialWarehouses.find(w=>w.id===warehouseId),batchesForProduct=batches.filter(b=>b.product_id===adjustProductId); 
 useEffect(()=>{if(warehouseId&&!materialWarehouses.some(w=>w.id===warehouseId))setWarehouseId("");},[warehouses,warehouseId]);
 async function applyAdjustment(e){e.preventDefault();setError("");const qty=Number(adjustQty);if(!company?.id||!warehouseId||!adjustProductId||!qty||qty<=0)return setError("Informe o material e uma quantidade maior que zero.");if(adjustType==="saida"&&batchesForProduct.length>0&&!adjustBatchId)return setError("Selecione o lote da saída.");if(!adjustLocationId)return setError(`Informe a localização ${adjustType==="entrada"?"de destino":"de origem"}.`);const {data:existing,error:findError}=await supabase.from("stock_levels").select("id,quantity").eq("company_id",company.id).eq("product_id",adjustProductId).eq("warehouse_id",warehouseId).eq("location_id",adjustLocationId).maybeSingle();if(findError)return setError(findError.message);const current=Number(existing?.quantity??0);if(adjustType==="saida"&&qty>current)return setError(`Saldo insuficiente nessa localização. Disponível: ${current.toLocaleString("pt-BR")}.`);const next=adjustType==="entrada"?current+qty:current-qty;const stockResult=existing?await supabase.from("stock_levels").update({quantity:next,updated_at:new Date().toISOString()}).eq("id",existing.id):await supabase.from("stock_levels").insert({company_id:company.id,product_id:adjustProductId,warehouse_id:warehouseId,location_id:adjustLocationId,quantity:next});if(stockResult.error)return setError(stockResult.error.message);if(adjustType==="entrada"&&adjustBatchNumber){const {error:batchError}=await supabase.from("stock_batches").insert({company_id:company.id,product_id:adjustProductId,warehouse_id:warehouseId,batch_number:adjustBatchNumber,expiry_date:adjustExpiryDate||null,quantity:qty});if(batchError)return setError(batchError.message);}if(adjustType==="saida"&&adjustBatchId){const batch=batches.find(b=>b.id===adjustBatchId);if(qty>Number(batch?.quantity??0))return setError("Quantidade maior que o saldo do lote selecionado.");const {error:batchError}=await supabase.from("stock_batches").update({quantity:Number(batch.quantity)-qty}).eq("id",adjustBatchId);if(batchError)return setError(batchError.message);}const {data:product}=await supabase.from("products").select("stock_quantity").eq("company_id",company.id).eq("id",adjustProductId).single();await supabase.from("products").update({stock_quantity:Math.max(0,Number(product?.stock_quantity??0)+(adjustType==="entrada"?qty:-qty))}).eq("company_id",company.id).eq("id",adjustProductId);await supabase.from("stock_movements").insert({company_id:company.id,product_id:adjustProductId,warehouse_id:warehouseId,movement_type:adjustType,quantity:qty,reference_type:"ajuste",notes:`${adjustType==="entrada"?"Entrada":"Saída"} manual — localização ${locations.find(l=>l.id===adjustLocationId)?.code||adjustLocationId}${adjustBatchNumber?` — lote ${adjustBatchNumber}`:""}`});setAdjustProductId("");setAdjustQty("");setAdjustLocationId("");setAdjustBatchNumber("");setAdjustExpiryDate("");setAdjustBatchId("");await Promise.all([loadLevels(warehouseId),loadExpiringBatches()]);}
 if(materialWarehouses.length===0)return <div style={styles.notice}>Nenhum almoxarifado de materiais ativo cadastrado. Cadastre em <Link to="/almoxarifados" style={styles.link}>Cadastro → Almoxarifados</Link>.</div>;
 const printDate=new Date().toLocaleString("pt-BR"),selectedTypeLabel=typeFilter?MATERIAL_LABEL[typeFilter]:"Todos os materiais";
 return <div className="stock-material-page"><style>{`@media print{@page{size:A4 landscape;margin:10mm 9mm 13mm}body{background:#fff!important;color:#111!important;font-family:Arial,Helvetica,sans-serif!important}.app-sidebar,nav,aside,.screen-only,.expiry-box,.filters,form,.error-box,.dim{display:none!important}.stock-material-page{padding:0!important;width:100%!important}.print-only{display:block!important}.print-report-header{display:grid!important;grid-template-columns:155px 1fr 225px;align-items:center;gap:16px;border-bottom:2px solid #222;padding-bottom:9px;margin-bottom:10px}.print-logo{width:140px;max-height:52px;object-fit:contain;object-position:left center}.print-title{margin:0;text-align:center;font-size:16px;font-weight:700;text-transform:uppercase}.print-subtitle{margin:4px 0 0;text-align:center;font-size:8.5px;color:#444}.print-meta{text-align:right;font-size:8.5px;line-height:1.45}.print-identification{display:grid!important;grid-template-columns:1.45fr .75fr .95fr .85fr;gap:7px;margin-bottom:9px}.print-field{border:1px solid #999;padding:5px 7px;min-height:27px}.print-label{display:block;font-size:6.8px;font-weight:700;text-transform:uppercase;color:#555;margin-bottom:2px}.print-value{font-size:8.5px;font-weight:600}.stock-material-page .tableWrap{border:1px solid #777!important;overflow:visible!important;border-radius:0!important}.stock-material-page table{table-layout:fixed!important;width:100%!important}.stock-material-page th,.stock-material-page td{color:#111!important;background:#fff!important;border:1px solid #aaa!important}.stock-material-page th{background:#eee!important;font-size:7.4px!important;padding:5px 4px!important;text-transform:uppercase;white-space:normal;line-height:1.15}.stock-material-page td{font-size:8.2px!important;padding:5px 4px!important;line-height:1.2}.stock-material-page th:nth-child(1),.stock-material-page td:nth-child(1){width:10%}.stock-material-page th:nth-child(2),.stock-material-page td:nth-child(2){width:23%}.stock-material-page th:nth-child(3),.stock-material-page td:nth-child(3){width:14%}.stock-material-page th:nth-child(4),.stock-material-page td:nth-child(4){width:12%}.stock-material-page th:nth-child(5),.stock-material-page td:nth-child(5){width:41%}.print-footer{display:flex!important;justify-content:space-between;border-top:1px solid #999;margin-top:8px;padding-top:4px;font-size:7px;color:#555}tr{page-break-inside:avoid}}@media screen{.print-only{display:none}}`}</style>
 <header style={styles.header} className="screen-only"><div style={styles.headerContent}><div><h1 style={styles.title}>Estoque de Materiais</h1><p style={styles.subtitle}>Relatório de posição de materiais, com controle por almoxarifado e localização.</p></div><button type="button" onClick={()=>window.print()} style={styles.printBtn}>Imprimir relatório</button></div></header>
 <div className="print-only print-report-header"><img className="print-logo" src={logoFull} alt="Logomarca ProdOS"/><div><h1 className="print-title">Relatório de Estoque de Materiais</h1><p className="print-subtitle">Posição de materiais por almoxarifado, localização e classificação</p></div><div className="print-meta"><strong>{company?.name||"Empresa"}</strong><br/>Emissão: {printDate}</div></div>
 <div className="print-only print-identification"><div className="print-field"><span className="print-label">Empresa</span><span className="print-value">{company?.name||"Não informado"}</span></div><div className="print-field"><span className="print-label">Almoxarifado</span><span className="print-value">{selectedWarehouse?.name||"Não selecionado"}</span></div><div className="print-field"><span className="print-label">Classificação</span><span className="print-value">{selectedTypeLabel}</span></div><div className="print-field"><span className="print-label">Materiais apresentados</span><span className="print-value">{levels.length} item(ns)</span></div></div>
 {expiringBatches.length>0&&<div style={styles.expiryBox} className="expiry-box"><strong>Atenção: lotes com vencimento próximo</strong>{expiringBatches.map(b=><div key={b.id} style={styles.expiryRow}><span>{b.products?.sku} — {b.products?.name} · lote {b.batch_number}</span><span>{Number(b.quantity).toLocaleString("pt-BR")} {b.products?.unit} · {b.warehouses?.name}</span><span>{new Date(b.expiry_date+"T00:00:00").toLocaleDateString("pt-BR")}</span></div>)}</div>}
 <div style={styles.filters} className="filters"><label style={styles.field}><span style={styles.fieldLabel}>Almoxarifado</span><select style={styles.input} value={warehouseId} onChange={e=>setWarehouseId(e.target.value)}><option value="">Selecione...</option>{materialWarehouses.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</select></label><label style={styles.field}><span style={styles.fieldLabel}>Tipo de Material</span><select style={styles.input} value={typeFilter} onChange={e=>setTypeFilter(e.target.value)}><option value="">Todos</option>{Object.entries(MATERIAL_LABEL).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label style={styles.field}><span style={styles.fieldLabel}>Material</span><select style={styles.input} value={productFilter} onChange={e=>setProductFilter(e.target.value)}><option value="">Todos</option>{filteredProducts.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select></label></div>
 {warehouseId&&<><form onSubmit={applyAdjustment} style={styles.form}><label style={styles.field}><span style={styles.fieldLabel}>Material</span><select style={styles.input} value={adjustProductId} onChange={e=>{setAdjustProductId(e.target.value);setAdjustBatchId("")}} required><option value="">Selecione...</option>{filteredProducts.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select></label><label style={styles.field}><span style={styles.fieldLabel}>Movimento</span><select style={styles.input} value={adjustType} onChange={e=>setAdjustType(e.target.value)}><option value="entrada">Entrada (+)</option><option value="saida">Saída (-)</option></select></label><label style={styles.field}><span style={styles.fieldLabel}>Quantidade</span><input style={styles.input} type="number" min="0.0001" step="any" value={adjustQty} onChange={e=>setAdjustQty(e.target.value)} required/></label><label style={styles.field}><span style={styles.fieldLabel}>{adjustType==="entrada"?"Localização destino":"Localização origem"}</span><select style={styles.input} value={adjustLocationId} onChange={e=>setAdjustLocationId(e.target.value)} required><option value="">Selecione...</option>{locations.map(l=><option key={l.id} value={l.id}>{l.code}</option>)}</select></label>{adjustType==="entrada"&&<><label style={styles.field}><span style={styles.fieldLabel}>Nº do lote</span><input style={styles.input} value={adjustBatchNumber} onChange={e=>setAdjustBatchNumber(e.target.value)} placeholder="Opcional"/></label><label style={styles.field}><span style={styles.fieldLabel}>Validade</span><input style={styles.input} type="date" value={adjustExpiryDate} onChange={e=>setAdjustExpiryDate(e.target.value)} disabled={!adjustBatchNumber}/></label></>}{adjustType==="saida"&&batchesForProduct.length>0&&<label style={styles.field}><span style={styles.fieldLabel}>Lote</span><select style={styles.input} value={adjustBatchId} onChange={e=>setAdjustBatchId(e.target.value)} required><option value="">Selecione...</option>{batchesForProduct.map(b=><option key={b.id} value={b.id}>{b.batch_number} — {Number(b.quantity).toLocaleString("pt-BR")} disp.</option>)}</select></label>}<div style={styles.actionRow}><button style={styles.addBtn} type="submit">Inserir movimento</button></div></form>{error&&<div style={styles.error} className="error-box">{error}</div>}{loading?<p style={styles.dim}>Carregando...</p>:levels.length===0?<p style={styles.dim}>Nenhum material cadastrado para os filtros selecionados.</p>:<div style={styles.tableWrap} className="tableWrap"><table style={styles.table}><thead><tr><th style={styles.th}>SKU</th><th style={styles.th}>Material</th><th style={styles.th}>Tipo</th><th style={styles.th}>Total</th><th style={styles.th}>Localizações</th></tr></thead><tbody>{levels.map(r=><tr key={r.id}><td style={styles.td}>{r.products.sku}</td><td style={styles.td}>{r.products.name}</td><td style={styles.td}>{MATERIAL_LABEL[r.products.type]}</td><td style={styles.td}><strong>{r.quantity.toLocaleString("pt-BR")} {r.products.unit||""}</strong></td><td style={styles.td}>{r.locations.length?r.locations.map(l=><div key={l.id||l.code}>{l.code}: <strong>{l.quantity.toLocaleString("pt-BR")}</strong> {r.products.unit||""}</div>):"—"}</td></tr>)}</tbody></table></div>}</>}
 <div className="print-only print-footer"><span>ProdOS — Relatório de Estoque de Materiais</span><span>Documento emitido pelo sistema</span></div></div>;
}
const styles={notice:{padding:24,background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",color:"var(--text)"},link:{color:"var(--amber)",textDecoration:"none",fontWeight:700},header:{marginBottom:18},headerContent:{display:"flex",justifyContent:"space-between",alignItems:"flex-start",gap:16},title:{fontFamily:"var(--font-display)",fontSize:22,margin:0},subtitle:{color:"var(--text-dim)",fontSize:13,margin:"6px 0 0",maxWidth:760,lineHeight:1.5},filters:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:12,padding:16,background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",marginBottom:16},form:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:12,padding:16,background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",marginBottom:16},field:{display:"flex",flexDirection:"column",gap:6},fieldLabel:{fontSize:12,fontWeight:700,color:"var(--text-dim)"},input:{width:"100%",minHeight:40,boxSizing:"border-box",padding:"8px 10px",border:"1px solid var(--line)",borderRadius:8,background:"var(--field)",color:"var(--text)"},actionRow:{gridColumn:"1 / -1",display:"flex",justifyContent:"flex-end",marginTop:2},addBtn:{minHeight:42,minWidth:190,padding:"0 20px",border:0,borderRadius:8,background:"#2563EB",color:"#fff",fontWeight:800,cursor:"pointer"},printBtn:{minHeight:42,padding:"0 18px",border:0,borderRadius:8,background:"#2563EB",color:"#fff",fontWeight:800,cursor:"pointer",whiteSpace:"nowrap"},error:{padding:12,marginBottom:16,borderRadius:8,border:"1px solid var(--red)",color:"var(--red)",background:"var(--panel)"},dim:{color:"var(--text-dim)"},expiryBox:{padding:14,marginBottom:16,background:"var(--panel)",border:"1px solid var(--amber)",borderRadius:"var(--radius)"},expiryRow:{display:"grid",gridTemplateColumns:"2fr 1fr 120px",gap:12,paddingTop:8,fontSize:13},tableWrap:{overflowX:"auto",background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)"},table:{width:"100%",borderCollapse:"collapse",fontSize:13},th:{textAlign:"left",padding:12,borderBottom:"1px solid var(--line)",color:"var(--text-dim)"},td:{padding:12,borderBottom:"1px solid var(--line)",verticalAlign:"top"}};
